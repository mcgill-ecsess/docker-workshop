services:
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - webapp-network

  backend:
    build: ./backend
    expose:
      - "8000"